diff --git a/app/scenes/Login/Provider.tsx b/app/scenes/Login/Provider.tsx
index ee350008..a3179940 100644
--- a/app/scenes/Login/Provider.tsx
+++ b/app/scenes/Login/Provider.tsx
@@ -84,7 +84,7 @@ class Provider extends React.Component<Props, State> {
                 <InputLarge
                   type="email"
                   name="email"
-                  placeholder="me@domain.com"
+                  placeholder="email@domaine.fr"
                   value={this.state.email}
                   onChange={this.handleChangeEmail}
                   disabled={this.state.isSubmitting}
@@ -93,12 +93,12 @@ class Provider extends React.Component<Props, State> {
                   short
                 />
                 <ButtonLarge type="submit" disabled={this.state.isSubmitting}>
-                  {t("Sign In")} →
+                  {t("S'identifier")} →
                 </ButtonLarge>
               </>
             ) : (
               <ButtonLarge type="submit" icon={<EmailIcon />} fullwidth>
-                {t("Continue with Email")}
+                {t("Accès membre")}
               </ButtonLarge>
             )}
           </Form>
@@ -113,7 +113,7 @@ class Provider extends React.Component<Props, State> {
           icon={<AuthLogo providerName={id} />}
           fullwidth
         >
-          {t("Continue with {{ authProviderName }}", {
+          {t("Accès {{ authProviderName }}", {
             authProviderName: name,
           })}
         </ButtonLarge>
diff --git a/app/scenes/Login/index.tsx b/app/scenes/Login/index.tsx
index bb7c54e6..dfbdca13 100644
--- a/app/scenes/Login/index.tsx
+++ b/app/scenes/Login/index.tsx
@@ -154,7 +154,7 @@ function Login() {
           </>
         ) : (
           <Heading centered>
-            {t("Login to {{ authProviderName }}", {
+            {t("Bienvenue sur {{ authProviderName }}", {
               authProviderName: config.name || "Outline",
             })}
           </Heading>
@@ -170,7 +170,7 @@ function Login() {
             {hasMultipleProviders && (
               <>
                 <Note>
-                  {t("You signed in with {{ authProviderName }} last time.", {
+                  {t("Vous vous êtes authentifié comme {{ authProviderName }} la dernière fois.", {
                     authProviderName: defaultProvider.name,
                   })}
                 </Note>
